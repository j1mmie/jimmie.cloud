import { ProjectDef } from '../../projects-core/project/ProjectDef'
import { Reflektive } from './Reflektive'

export const reflektiveProjectDef:ProjectDef = {
  id:    'reflektive',
  title: 'Reflektive',
  comp:   Reflektive,
}
